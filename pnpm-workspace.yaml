---
# pnpm-workspace.yaml
# Minimal single-package workspace (root package.json has workspaces: null).
# allowBuilds lists native deps whose postinstall scripts we trust to run.
allowBuilds:
  esbuild: true
  sharp: true
