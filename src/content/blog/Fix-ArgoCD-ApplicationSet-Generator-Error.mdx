---
draft: false
title: "Fix ArgoCD ApplicationSet Generator Error"
seoTitle: "Fix ArgoCD ApplicationSet Generator Error"
snippet: "ApplicationSet not generating apps? Debug Git, list, cluster, and matrix generators with proper templates."
description: "ApplicationSet not generating apps? Debug Git, list, cluster, and matrix generators with proper templates. Practical guide with examples and."
publishDate: "2026-04-03"
lastModified: "2026-04-03"
category: "DevOps"
author: "Luca Berton"
tags:
  - "argocd"
  - "gitops"
  - "kubernetes"
  - "troubleshooting"
  - "devops"
image:
  src: "/blog/books/Practical RHEL AI Dyone Dekker.jpg"
  alt: "Fix ArgoCD ApplicationSet Generator Error"
---
## The Problem

ArgoCD ApplicationSet fails with generator errors. Applications are not created or are created incorrectly.

## Root Cause

ApplicationSet generators fail when:
- Git repository is unreachable
- Directory path does not exist
- Template variables are undefined
- Generator produces empty results

## Fix

### Check ApplicationSet Controller Logs

```bash
kubectl logs -n argocd deployment/argocd-applicationset-controller --tail=100
```

### Common Fixes

**Git generator — repository not found:**
```yaml
spec:
  generators:
    - git:
        repoURL: https://github.com/org/repo.git
        revision: HEAD
        directories:
          - path: "apps/*"   # Ensure this path exists
```

**List generator — validate entries:**
```yaml
spec:
  generators:
    - list:
        elements:
          - cluster: production
            url: https://kubernetes.default.svc
          - cluster: staging
            url: https://staging-cluster:6443
```

**Matrix generator — check both sub-generators independently first.**

### Validate Before Applying

```bash
# Dry-run to see what applications would be created
kubectl apply --dry-run=server -f applicationset.yaml
```

## Related Resources

- [ArgoCD Cheat Sheet](/blog/argocd-cheat-sheet-2026/)
- [Fix ArgoCD App of Apps Sync Issues](/blog/fix-argocd-app-of-apps-sync-issues/)
