---
draft: false
title: "How to Install Ansible on Windows WSL2"
seoTitle: "How to Install Ansible on Windows WSL2"
snippet: "Step-by-step guide to install Ansible on Windows WSL2. Covers package installation, pip install, version management, and first playbook setup."
publishDate: "2026-04-03"
lastModified: "2026-04-03"
category: "Automation"
author: "Luca Berton"
tags:
  - "ansible"
  - "installation"
  - "automation"
  - "devops"
  - "tutorial"
image:
  src: "/blog/books/Practical RHEL AI Dyone Dekker.jpg"
  alt: "How to Install Ansible on Windows WSL2"
---
Here is how to install Ansible on Windows using WSL2 in 2026. Ansible does not run natively on Windows.

## Step 1: Install WSL2

```powershell
wsl --install -d Ubuntu-24.04
```

Restart and complete the Ubuntu setup.

## Step 2: Install Ansible

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y ansible
```

## Verify

```bash
ansible --version
ansible localhost -m ping
```

## Alternative: pip

```bash
sudo apt install -y python3-pip python3-venv
python3 -m venv ~/ansible-venv
source ~/ansible-venv/bin/activate
pip install ansible
```

## Managing Windows Hosts from WSL2

```bash
pip install pywinrm
ansible-galaxy collection install community.windows
```

## Related Resources

- [Ansible Cheat Sheet](/blog/ansible-cheat-sheet-2026/)
- [Install Ansible on Ubuntu 24.04](/blog/How-to-Install-Ansible-on-ubuntu-24-04/)
