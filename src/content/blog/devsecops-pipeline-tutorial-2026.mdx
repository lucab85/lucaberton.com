---
draft: false
title: "DevSecOps Pipeline Tutorial: Shift Left Security in CI/CD (2026)"
snippet: "Build a DevSecOps pipeline. SAST, DAST, SCA, container scanning, IaC scanning, and secrets detection in GitHub Actions and GitLab CI."
description: "Build a DevSecOps pipeline. SAST, DAST, SCA, container scanning, IaC scanning, and secrets detection in GitHub Actions and GitLab CI."
image:
  src: "/blog/thumbnails/devsecops-pipeline-tutorial-2026.jpg"
  alt: "DevSecOps Pipeline Tutorial: Shift Left Security in CI/CD (2026)"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "DevOps"
tags: ["devsecops", "Tutorial", "2026", "DevOps", "DevOps"]
---

Security is not a feature you bolt on — it is a practice you build in from day one.

## Step-by-Step Guide

### Shift Left: Security in CI/CD

Catch vulnerabilities before they reach production:

```bash
# Scan container images
trivy image myapp:latest

# Scan IaC files
trivy config ./terraform/

# Scan for secrets
gitleaks detect --source . --verbose
```

### Container Security Basics

```dockerfile
# Run as non-root
FROM node:22-alpine
RUN addgroup -S app && adduser -S app -G app
USER app

# Read-only filesystem
# docker run --read-only --tmpfs /tmp myapp:latest
```

### Kubernetes Security

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: app
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
        readOnlyRootFilesystem: true
```

## Related Resources

- [Trivy vs Grype](/blog/trivy-vs-grype-2026/)
- [Vault vs AWS Secrets Manager](/blog/vault-vs-aws-secrets-manager/)
- [SELinux Cheat Sheet](/blog/selinux-cheat-sheet-2026/)
- [Kubernetes RBAC Guide](/blog/kubernetes-rbac-guide-roles-bindings-2026/)

## About the Author

I am Luca Berton, AI and Cloud Advisor with 8 published books on automation, Kubernetes, and AI. [Book a consultation](https://calendly.com/lucaberton/) to discuss your devsecops strategy.
