---
draft: false
title: "etcdctl Cheat Sheet: Kubernetes etcd Commands"
snippet: "etcdctl cheat sheet for Kubernetes cluster management. Get/put keys, snapshot backup and restore, member management, health checks, and compaction commands."
description: "etcdctl cheat sheet for Kubernetes etcd operations. Key-value operations, snapshot backup/restore, cluster member management, health checks."
image:
  src: "/blog/thumbnails/etcdctl-cheat-sheet-2026.jpg"
  alt: "etcdctl Cheat Sheet: Kubernetes etcd Commands"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["Comparison", "Platform Engineering", "DevOps", "2026"]
---
A quick reference for etcdctl — the etcd command-line client. Bookmark this page.

This is a companion to the [etcd Cheat Sheet](/blog/etcd-cheat-sheet-2026/), focused on etcdctl commands specifically for Kubernetes cluster administration.

## Setup

```bash
# Set API version (always use v3)
export ETCDCTL_API=3

# For Kubernetes clusters
export ETCDCTL_ENDPOINTS=https://127.0.0.1:2379
export ETCDCTL_CACERT=/etc/kubernetes/pki/etcd/ca.crt
export ETCDCTL_CERT=/etc/kubernetes/pki/etcd/server.crt
export ETCDCTL_KEY=/etc/kubernetes/pki/etcd/server.key

# Or pass inline
etcdctl --endpoints=https://127.0.0.1:2379 \
  --cacert=/etc/kubernetes/pki/etcd/ca.crt \
  --cert=/etc/kubernetes/pki/etcd/server.crt \
  --key=/etc/kubernetes/pki/etcd/server.key \
  endpoint health
```

## Health and Status

```bash
etcdctl endpoint health
etcdctl endpoint health --cluster
etcdctl endpoint status --write-out=table
etcdctl member list --write-out=table
```

## Backup and Restore

```bash
# Backup
etcdctl snapshot save /backup/etcd-$(date +%Y%m%d-%H%M).db

# Verify backup
etcdctl snapshot status /backup/etcd-20260413.db --write-out=table

# Restore (stop etcd first!)
etcdctl snapshot restore /backup/etcd-20260413.db \
  --data-dir=/var/lib/etcd-restored
```

## Key Operations

```bash
etcdctl get / --prefix --keys-only | head -20
etcdctl get /registry/pods/default --prefix --keys-only
etcdctl get /registry/deployments --prefix --keys-only | wc -l
```

## Maintenance

```bash
# Check DB size
etcdctl endpoint status --write-out=json | python3 -m json.tool

# Compact and defrag
rev=$(etcdctl endpoint status --write-out=json | python3 -c "import sys,json; print(json.load(sys.stdin)[0]['Status']['header']['revision'])")
etcdctl compact $rev
etcdctl defrag --cluster
```

## Tips and Tricks

- Schedule daily etcd backups — it is the single source of truth for your cluster
- Monitor etcd disk latency: `etcdctl check perf`
- Use `--write-out=table` for human-readable output
- Keep etcd data on fast SSD/NVMe storage
- Set alarms: `etcdctl alarm list` and `etcdctl alarm disarm`

## Related Resources

- [etcd Cheat Sheet](/blog/etcd-cheat-sheet-2026/)
- [Kubernetes Cheat Sheet](/blog/kubernetes-cheat-sheet-kubectl-commands-2026/)
- [Helm Cheat Sheet](/blog/helm-cheat-sheet-2026/)
