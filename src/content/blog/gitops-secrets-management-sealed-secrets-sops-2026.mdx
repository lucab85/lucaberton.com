---
title: "GitOps Secrets: Sealed Secrets vs SOPS vs External Secrets"
snippet: "Compare three approaches to secrets in GitOps workflows — Bitnami Sealed Secrets, Mozilla SOPS with age/KMS, and External Secrets Operator."
description: "Compare three approaches to secrets in GitOps workflows — Bitnami Sealed Secrets, Mozilla SOPS with age/KMS, and External Secrets Operator."
publishDate: "2026-06-05"
lastModified: "2026-06-05"
author: "Luca Berton"
category: "DevOps"
tags: ['Security', 'GitOps', 'Kubernetes']
image:
  src: "/blog/thumbnails/gitops-secrets-management-sealed-secrets-sops-2026.jpg"
  alt: "GitOps Secrets: Sealed Secrets vs SOPS vs External Secrets"
draft: false
---

import BlogPromoCard from '../../components/BlogPromoCard.astro';
## The Problem: Secrets in Git

GitOps means everything in Git. But secrets can't be stored in plaintext. Three solutions dominate:

| Solution | Encryption | Key Management | GitOps Native |
|----------|-----------|----------------|---------------|
| Sealed Secrets | Asymmetric (RSA) | In-cluster controller | ✅ |
| SOPS | Symmetric (age/KMS) | External (AWS KMS, GCP, age) | ⚠️ (needs decrypt step) |
| External Secrets | None (reference only) | External vault | ✅ |

## 1. Bitnami Sealed Secrets

Encrypts secrets client-side; only the in-cluster controller can decrypt.

```bash
# Install controller
helm install sealed-secrets sealed-secrets/sealed-secrets -n kube-system

# Encrypt a secret
kubectl create secret generic db-creds \
  --from-literal=password=super-secret \
  --dry-run=client -o yaml | \
  kubeseal --format yaml > sealed-db-creds.yaml
```

```yaml
# This is safe to commit to Git!
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: db-creds
spec:
  encryptedData:
    password: AgBy3i4OJSWK+PiTySYZZA9rO...  # RSA-encrypted
```

### Pros/Cons

| ✅ Pros | ❌ Cons |
|---------|---------|
| Simple workflow | Secrets re-encrypted per cluster |
| No external dependencies | Key rotation requires re-seal all |
| Native K8s Secret output | Can't use outside Kubernetes |
| Commit encrypted secrets to Git | Losing the private key = lost secrets |

## 2. Mozilla SOPS

Encrypts YAML/JSON values in-place using age, AWS KMS, GCP KMS, or Azure Key Vault.

```bash
# Create age key
age-keygen -o key.txt

# Encrypt
sops --encrypt --age age1... secret.yaml > secret.enc.yaml

# Decrypt (CI/CD or Flux)
sops --decrypt secret.enc.yaml | kubectl apply -f -
```

```yaml
# Encrypted file — keys visible, values encrypted
apiVersion: v1
kind: Secret
metadata:
    name: db-creds
data:
    password: ENC[AES256_GCM,data:kE3l...,type:str]
sops:
    age:
        - recipient: age1...
          enc: |
            -----BEGIN AGE ENCRYPTED FILE-----
```

### With Flux CD

```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age  # Age private key stored as K8s secret
```

### Pros/Cons

| ✅ Pros | ❌ Cons |
|---------|---------|
| Works anywhere (not K8s-specific) | Extra tooling in CI/CD |
| Multiple key recipients | Need SOPS binary everywhere |
| AWS/GCP KMS integration | More complex key management |
| Diff-friendly (keys visible) | Not native to Argo CD (plugin needed) |

## 3. External Secrets Operator

Doesn't store secrets in Git at all — just references.

```yaml
# Only the reference is in Git (no secret data)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: db-creds
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
  data:
    - secretKey: password
      remoteRef:
        key: production/database
        property: password
```

### Pros/Cons

| ✅ Pros | ❌ Cons |
|---------|---------|
| No secrets in Git (even encrypted) | Requires external vault |
| Auto-rotation | Runtime dependency on vault |
| Audit trail in vault | More infrastructure to manage |
| Works with any vault | Cluster needs vault access |

## Decision Matrix

| Requirement | Sealed Secrets | SOPS | External Secrets |
|-------------|---------------|------|------------------|
| Small team, simple setup | ✅ Best | ⚠️ | ❌ Overkill |
| Multi-cloud/multi-cluster | ❌ | ✅ | ✅ |
| Compliance (secret never in Git) | ❌ | ❌ | ✅ Best |
| Existing HashiCorp Vault | ❌ | ⚠️ | ✅ Best |
| Air-gapped environments | ✅ | ✅ | ❌ |
| Non-Kubernetes workloads | ❌ | ✅ | ❌ |

## My Recommendation

For most organizations:

1. **Start with Sealed Secrets** — lowest barrier to entry
2. **Graduate to External Secrets** — when you have a vault and need rotation
3. **Use SOPS** — for non-Kubernetes config or multi-tool environments

<BlogPromoCard />

## Related Articles

- [External Secrets Operator Deep Dive](/blog/external-secrets-operator-kubernetes-vault-2026/)
- [Cilium: eBPF Security](/blog/cilium-ebpf-kubernetes-networking-security-2026/)
- [Argo CD: GitOps Deployment](/blog/argo-cd-gitops-kubernetes-deployment-2026/)
