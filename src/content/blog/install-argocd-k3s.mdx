---
draft: false
title: "Install ArgoCD on K3s (Step-by-Step 2026)"
snippet: "Step-by-step guide to install ArgoCD on K3s. Copy-paste commands, verify installation, and configure for production."
description: "How to install ArgoCD on K3s in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-argocd-k3s.jpg"
  alt: "Install ArgoCD on K3s"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["ArgoCD", "Installation", "K3s", "Tutorial", "DevOps"]
---
Here is how to install ArgoCD on K3s in 2026. Tested and verified.

## Prerequisites

- K3s cluster running ([Install K3s on Ubuntu](/blog/install-k3s-ubuntu/))

## Install ArgoCD

```bash
kubectl create namespace argocd
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
kubectl wait --for=condition=ready pod -l app.kubernetes.io/name=argocd-server -n argocd --timeout=120s
```

## Expose via K3s Traefik Ingress

```yaml
# argocd-ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server
  namespace: argocd
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  rules:
    - host: argocd.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 443
```

```bash
kubectl apply -f argocd-ingress.yaml
```

## Get Admin Password

```bash
kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d
echo
```

## Related Resources

- [ArgoCD Cheat Sheet](/blog/argocd-cheat-sheet-2026/)
- [Install ArgoCD on Kubernetes](/blog/install-argocd-kubernetes/)
- [Install K3s on Ubuntu](/blog/install-k3s-ubuntu/)
