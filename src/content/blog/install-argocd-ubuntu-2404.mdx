---
draft: false
title: "Install ArgoCD on Ubuntu 24.04 LTS: Complete Guide (2026)"
snippet: "Step-by-step guide to install ArgoCD on Ubuntu 24.04 LTS. Repository setup, installation, configuration, and verification."
description: "Install ArgoCD on Ubuntu 24.04 LTS in 2026. Official repository setup, package installation, post-install configuration, and troubleshooting."
image:
  src: "/blog/thumbnails/install-argocd-ubuntu-2404.jpg"
  alt: "Install ArgoCD on Ubuntu 24.04 LTS"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "DevOps"
tags: ["ArgoCD", "Ubuntu 24.04 LTS", "Installation", "Linux", "DevOps"]
---

Here is how to install the ArgoCD CLI on Ubuntu 24.04 LTS. Tested and verified.

> **Scope:** This guide installs the `argocd` command-line client only — a single static binary with no daemon, no systemd service, and no dedicated system group. The ArgoCD *server* runs as pods inside a Kubernetes cluster. If you don't already have one running, see [Install ArgoCD on Kubernetes](/blog/install-argocd-kubernetes/) or [Install ArgoCD on K3s](/blog/install-argocd-k3s/) first.

## Prerequisites

- Ubuntu 24.04 LTS installed and updated
- Root or sudo access
- Internet connection
- An existing ArgoCD server to log into (optional to install the CLI itself, required to actually use it)

## Step 1: Update System

```bash
sudo apt update && sudo apt upgrade -y
```

## Step 2: Detect Your Architecture and Download the CLI Binary

The ArgoCD CLI has no `apt` package and no official APT repository — the project does not publish one. The supported installation method is downloading the prebuilt static binary for your CPU architecture directly from the [ArgoCD GitHub releases page](https://github.com/argoproj/argo-cd/releases). This step is identical across every Linux distribution; there is nothing Ubuntu-specific about it.

```bash
# Ensure curl is available
sudo apt install -y curl

# Detect CPU architecture (x86_64 -> amd64, aarch64 -> arm64)
case "$(uname -m)" in
  x86_64)  ARGOCD_ARCH="amd64" ;;
  aarch64) ARGOCD_ARCH="arm64" ;;
  *) echo "Unsupported architecture: $(uname -m)" >&2; exit 1 ;;
esac

# Download the matching binary
curl -sSL -o argocd-linux-$ARGOCD_ARCH "https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-$ARGOCD_ARCH"
```

## Step 3: Install the Binary

```bash
sudo install -m 555 argocd-linux-$ARGOCD_ARCH /usr/local/bin/argocd
rm argocd-linux-$ARGOCD_ARCH
```

## Step 4: Verify Installation

```bash
argocd version --client
```

Expected output shows only the client version — there is no local server for it to compare against.

## Step 5: Shell Completion and Connecting to a Server

Optional: enable bash completion for the `argocd` command.

```bash
argocd completion bash | sudo tee /etc/bash_completion.d/argocd > /dev/null
```

The binary alone doesn't do anything until it's pointed at a running ArgoCD server:

```bash
argocd login <your-argocd-server>:443
```

If you don't have a server yet, deploy one first: [Install ArgoCD on Kubernetes](/blog/install-argocd-kubernetes/) or [Install ArgoCD on K3s](/blog/install-argocd-k3s/).

## Troubleshooting

### command not found: argocd

`/usr/local/bin` isn't on every shell's `PATH` by default. Confirm it's included, or open a new shell session:

```bash
echo $PATH | tr ':' '\n' | grep /usr/local/bin
which argocd
```

### Binary Won't Run / Permission Denied

If you copied the binary manually instead of using `install -m 555`, it may not be marked executable:

```bash
sudo chmod +x /usr/local/bin/argocd
```

### x509: certificate signed by unknown authority

This happens when `argocd login` connects to a server presenting a self-signed certificate. For a lab or test server, skip verification:

```bash
argocd login <your-argocd-server>:443 --insecure
```

For production, import the server's CA certificate into your trust store instead of using `--insecure`.

## Uninstall

The CLI is a single file, not something installed through a package manager — remove it directly:

```bash
sudo rm /usr/local/bin/argocd
```

## Related Guides

- [Install ArgoCD on Kubernetes](/blog/install-argocd-kubernetes/)
- [Install ArgoCD on K3s](/blog/install-argocd-k3s/)
- [ArgoCD Cheat Sheet](/blog/argocd-cheat-sheet-2026/)
- [ArgoCD Tutorial](/blog/fix-argocd-app-of-apps-sync-issues/)
- [Docker vs Kubernetes](/blog/docker-vs-kubernetes-when-you-need-which/)

## About the Author

I am Luca Berton, AI and Cloud Advisor. [Book a consultation](https://calendly.com/lucaberton/) to get help with your infrastructure setup.
