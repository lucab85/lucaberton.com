---
draft: false
title: "Install Grafana on Ubuntu (Step-by-Step 2026)"
snippet: "Step-by-step guide to install Grafana on Ubuntu. Copy-paste commands, verify installation, and configure for production."
description: "How to install Grafana on Ubuntu in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-grafana-ubuntu.jpg"
  alt: "Install Grafana on Ubuntu"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "DevOps"
tags: ["Grafana", "Installation", "Ubuntu", "Tutorial", "DevOps"]
---
Here is how to install Grafana on Ubuntu in 2026. Tested and verified.

## Method 1: APT Repository (Recommended)

```bash
# Install prerequisites
sudo apt-get install -y apt-transport-https software-properties-common wget

# Add Grafana GPG key and repository
sudo mkdir -p /etc/apt/keyrings/
wget -q -O - https://apt.grafana.com/gpg.key | gpg --dearmor | sudo tee /etc/apt/keyrings/grafana.gpg > /dev/null
echo "deb [signed-by=/etc/apt/keyrings/grafana.gpg] https://apt.grafana.com stable main" | sudo tee /etc/apt/sources.list.d/grafana.list

# Install Grafana
sudo apt-get update
sudo apt-get install -y grafana

# Start and enable
sudo systemctl daemon-reload
sudo systemctl start grafana-server
sudo systemctl enable grafana-server
```

## Access Grafana

Open http://localhost:3000 in your browser.

- **Username**: admin
- **Password**: admin (you will be prompted to change it)

## Method 2: Docker

```bash
docker run -d --name grafana -p 3000:3000 grafana/grafana-oss:latest
```

## Method 3: Helm (Kubernetes)

```bash
helm repo add grafana https://grafana.github.io/helm-charts
helm install grafana grafana/grafana --namespace monitoring --create-namespace
```

## Related Resources

- [Grafana Dashboard Tutorial](/blog/grafana-dashboard-tutorial-2026/)
- [Install Prometheus on Ubuntu](/blog/install-prometheus-ubuntu/)
- [Grafana vs Datadog](/blog/grafana-vs-datadog/)
