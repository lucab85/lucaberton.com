---
draft: false
title: "Install Helm on Ubuntu (Step-by-Step 2026)"
snippet: "Step-by-step guide to install Helm on Ubuntu. Copy-paste commands, verify installation, and configure for production."
description: "How to install Helm on Ubuntu in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-helm-ubuntu.jpg"
  alt: "Install Helm on Ubuntu"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["Helm", "Installation", "Ubuntu", "Tutorial", "DevOps"]
---
Here is how to install Helm on Ubuntu in 2026. Tested and verified.

## Prerequisites

- Ubuntu 22.04 or 24.04 LTS
- `kubectl` configured with cluster access
- sudo privileges

## Method 1: Official Script (Recommended)

```bash
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
```

## Method 2: APT Repository

```bash
curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
sudo apt-get update
sudo apt-get install helm
```

## Method 3: Snap

```bash
sudo snap install helm --classic
```

## Verify Installation

```bash
helm version
helm env
```

## Post-Installation Setup

```bash
# Add common repositories
helm repo add stable https://charts.helm.sh/stable
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

# Test with a chart
helm search repo nginx
```

## Quick Start

```bash
# Install a chart
helm install my-nginx bitnami/nginx

# List releases
helm list

# Uninstall
helm uninstall my-nginx
```

## Related Resources

- [Helm Cheat Sheet](/blog/helm-cheat-sheet-2026/)
- [Install Helm on macOS](/blog/install-helm-macos/)
- [Install Helm on Windows](/blog/install-helm-windows/)
