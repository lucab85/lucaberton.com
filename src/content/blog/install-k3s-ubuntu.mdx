---
draft: false
title: "Install K3s on Ubuntu: Lightweight Kubernetes (Step-by-Step 2026)"
snippet: "Step-by-step guide to install K3s on Ubuntu. Copy-paste commands, verify installation, and configure for production."
description: "How to install K3s on Ubuntu in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-k3s-ubuntu.jpg"
  alt: "Install K3s on Ubuntu: Lightweight Kubernetes"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["K3s", "Installation", "Ubuntu", "Tutorial", "DevOps"]
---
Here is how to install K3s on Ubuntu in 2026. Tested and verified.

## Prerequisites

- Ubuntu 22.04 or 24.04 LTS
- 2 GB RAM minimum (4 GB recommended)
- 2 CPU cores minimum

## Install K3s Server (Control Plane)

```bash
curl -sfL https://get.k3s.io | sh -
```

That is it. One command. K3s is now running.

## Verify

```bash
sudo k3s kubectl get nodes
sudo k3s kubectl get pods -A

# Copy kubeconfig for regular kubectl use
mkdir -p ~/.kube
sudo cp /etc/rancher/k3s/k3s.yaml ~/.kube/config
sudo chown $(id -u):$(id -g) ~/.kube/config
export KUBECONFIG=~/.kube/config

kubectl get nodes
```

## Add Worker Nodes

On the server, get the token:

```bash
sudo cat /var/lib/rancher/k3s/server/node-token
```

On each worker node:

```bash
curl -sfL https://get.k3s.io | K3S_URL=https://SERVER_IP:6443 K3S_TOKEN=YOUR_TOKEN sh -
```

## Install with Custom Options

```bash
# Without Traefik (use your own ingress)
curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="--disable traefik" sh -

# Without ServiceLB
curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="--disable servicelb --disable traefik" sh -

# With specific version
curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION="v1.31.0+k3s1" sh -
```

## Uninstall

```bash
# Server
/usr/local/bin/k3s-uninstall.sh

# Agent
/usr/local/bin/k3s-agent-uninstall.sh
```

## Related Resources

- [K3s vs K8s](/blog/k3s-vs-k8s-2026/)
- [Install K3s on Raspberry Pi](/blog/install-k3s-raspberry-pi/)
- [Install ArgoCD on K3s](/blog/install-argocd-k3s/)
