---
draft: false
title: "Install kubectl on macOS (Step-by-Step 2026)"
snippet: "Step-by-step guide to install kubectl on macOS. Copy-paste commands, verify installation, and configure for production."
description: "How to install kubectl on macOS in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-kubectl-macos.jpg"
  alt: "Install kubectl on macOS"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["kubectl", "Installation", "macOS", "Tutorial", "DevOps"]
---
Here is how to install kubectl on macOS in 2026. Tested and verified.

## Method 1: Homebrew (Recommended)

```bash
brew install kubectl
```

## Method 2: Direct Download

```bash
# Intel Mac
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/darwin/amd64/kubectl"

# Apple Silicon (M1/M2/M3/M4)
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/darwin/arm64/kubectl"

chmod +x kubectl
sudo mv kubectl /usr/local/bin/
```

## Verify

```bash
kubectl version --client
```

## Enable Shell Completion

```bash
# Zsh (default on macOS)
echo 'source <(kubectl completion zsh)' >> ~/.zshrc
echo 'alias k=kubectl' >> ~/.zshrc
source ~/.zshrc
```

## Related Resources

- [Kubernetes Cheat Sheet](/blog/kubernetes-cheat-sheet-kubectl-commands-2026/)
- [Install Helm on macOS](/blog/install-helm-macos/)
- [Install kubectl on Ubuntu](/blog/install-kubectl-ubuntu/)
