---
draft: false
title: "Install kubectl on Ubuntu (Step-by-Step 2026)"
seoTitle: "Install kubectl on Ubuntu: 3 Quick Methods (2026)"
snippet: "Step-by-step guide to install kubectl on Ubuntu. Copy-paste commands, verify installation, and configure for production."
description: "Install kubectl on Ubuntu with apt, direct download, or snap. Includes verification and shell completion for production use."
image:
  src: "/blog/thumbnails/install-kubectl-ubuntu.jpg"
  alt: "Install kubectl on Ubuntu"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["kubectl", "Installation", "Ubuntu", "Tutorial", "DevOps"]
---
Three methods to install kubectl on Ubuntu: apt repository (recommended), direct download, or snap. All tested and ready to use.

## Method 1: APT Repository (Recommended)

```bash
# Add Kubernetes APT repository
sudo apt-get update && sudo apt-get install -y apt-transport-https ca-certificates curl gnupg
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.31/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.31/deb/ /' | sudo tee /etc/apt/sources.list.d/kubernetes.list

# Install kubectl
sudo apt-get update
sudo apt-get install -y kubectl
```

## Method 2: Direct Download

```bash
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
rm kubectl
```

## Method 3: Snap

```bash
sudo snap install kubectl --classic
```

## Verify

```bash
kubectl version --client
kubectl cluster-info  # If connected to a cluster
```

## Enable Shell Completion

```bash
# Bash
echo 'source <(kubectl completion bash)' >> ~/.bashrc
echo 'alias k=kubectl' >> ~/.bashrc
echo 'complete -o default -F __start_kubectl k' >> ~/.bashrc
source ~/.bashrc
```

## Related Resources

- [Kubernetes Cheat Sheet](/blog/kubernetes-cheat-sheet-kubectl-commands-2026/)
- [Install kubectl on macOS](/blog/install-kubectl-macos/)
- [Install kubectl on Windows](/blog/install-kubectl-windows/)
