---
draft: false
title: "Install Kubernetes on openSUSE Leap (Step-by-Step 2026)"
snippet: "Step-by-step guide to install Kubernetes on openSUSE Leap. Copy-paste commands, verify installation, and configure for production."
description: "How to install Kubernetes on openSUSE Leap in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-kubernetes-opensuse-leap.jpg"
  alt: "Install Kubernetes on openSUSE Leap"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["Kubernetes", "Installation", "openSUSE Leap", "Tutorial", "DevOps"]
---
Here is how to install Kubernetes on openSUSE Leap using kubeadm in 2026. Tested and verified.

## Prerequisites

```bash
sudo swapoff -a
sudo sed -i '/ swap / s/^/#/' /etc/fstab

cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF
sudo modprobe overlay && sudo modprobe br_netfilter

cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF
sudo sysctl --system
```

## Install containerd

```bash
sudo zypper install -y containerd
containerd config default | sudo tee /etc/containerd/config.toml
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' /etc/containerd/config.toml
sudo systemctl restart containerd
sudo systemctl enable containerd
```

## Install Kubernetes

```bash
# Add Kubernetes repository
sudo zypper addrepo --gpgcheck https://pkgs.k8s.io/core:/stable:/v1.31/rpm/ kubernetes
sudo zypper --gpg-auto-import-keys refresh

sudo zypper install -y kubelet kubeadm kubectl
sudo systemctl enable --now kubelet
```

## Initialize Cluster

```bash
sudo kubeadm init --pod-network-cidr=10.244.0.0/16

mkdir -p $HOME/.kube
sudo cp /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config

kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
```

## Related Resources

- [Kubernetes Cheat Sheet](/blog/kubernetes-cheat-sheet-kubectl-commands-2026/)
- [K3s vs K8s](/blog/k3s-vs-k8s-2026/)
- [Install K3s on Ubuntu](/blog/install-k3s-ubuntu/)
