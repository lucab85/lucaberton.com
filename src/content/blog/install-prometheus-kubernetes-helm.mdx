---
draft: false
title: "Install Prometheus on Kubernetes with Helm"
snippet: "Step-by-step guide to install Prometheus on Kubernetes. Copy-paste commands, verify installation, and configure for production."
description: "How to install Prometheus on Kubernetes in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-prometheus-kubernetes-helm.jpg"
  alt: "Install Prometheus on Kubernetes with Helm"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Platform Engineering"
tags: ["Prometheus", "Installation", "Kubernetes", "Tutorial", "DevOps"]
---
Here is how to install Prometheus on Kubernetes using Helm in 2026. Tested and verified.

## Install kube-prometheus-stack

This installs Prometheus, Grafana, AlertManager, and node exporters:

```bash
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

helm install monitoring prometheus-community/kube-prometheus-stack \
  --namespace monitoring \
  --create-namespace
```

## Verify

```bash
kubectl get pods -n monitoring
kubectl get svc -n monitoring
```

## Access Dashboards

```bash
# Grafana
kubectl port-forward svc/monitoring-grafana -n monitoring 3000:80
# Username: admin, Password:
kubectl get secret monitoring-grafana -n monitoring -o jsonpath="{.data.admin-password}" | base64 -d

# Prometheus
kubectl port-forward svc/monitoring-kube-prometheus-prometheus -n monitoring 9090:9090

# AlertManager
kubectl port-forward svc/monitoring-kube-prometheus-alertmanager -n monitoring 9093:9093
```

## Custom Values

```yaml
# values.yaml
prometheus:
  prometheusSpec:
    retention: 30d
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi

grafana:
  adminPassword: "your-secure-password"
  ingress:
    enabled: true
    hosts:
      - grafana.example.com
```

```bash
helm upgrade monitoring prometheus-community/kube-prometheus-stack \
  -n monitoring -f values.yaml
```

## Related Resources

- [Prometheus Tutorial](/blog/prometheus-tutorial-complete-guide-2026/)
- [Prometheus PromQL Cheat Sheet](/blog/prometheus-query-cheat-sheet/)
- [Grafana Dashboard Tutorial](/blog/grafana-dashboard-tutorial-2026/)
