---
draft: false
title: "Install Prometheus on Ubuntu (Step-by-Step 2026)"
snippet: "Step-by-step guide to install Prometheus on Ubuntu. Copy-paste commands, verify installation, and configure for production."
description: "How to install Prometheus on Ubuntu in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-prometheus-ubuntu.jpg"
  alt: "Install Prometheus on Ubuntu"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "DevOps"
tags: ["Prometheus", "Installation", "Ubuntu", "Tutorial", "DevOps"]
---
Here is how to install Prometheus on Ubuntu in 2026. Tested and verified.

## Method 1: Binary Installation

```bash
# Download latest release
PROM_VERSION="2.53.0"
wget https://github.com/prometheus/prometheus/releases/download/v${PROM_VERSION}/prometheus-${PROM_VERSION}.linux-amd64.tar.gz
tar xvfz prometheus-${PROM_VERSION}.linux-amd64.tar.gz
cd prometheus-${PROM_VERSION}.linux-amd64

# Install
sudo cp prometheus promtool /usr/local/bin/
sudo mkdir -p /etc/prometheus /var/lib/prometheus
sudo cp prometheus.yml /etc/prometheus/
```

## Create systemd Service

```ini
# /etc/systemd/system/prometheus.service
[Unit]
Description=Prometheus
After=network-online.target

[Service]
User=prometheus
ExecStart=/usr/local/bin/prometheus \
    --config.file=/etc/prometheus/prometheus.yml \
    --storage.tsdb.path=/var/lib/prometheus/ \
    --web.console.templates=/etc/prometheus/consoles \
    --web.console.libraries=/etc/prometheus/console_libraries
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo useradd --no-create-home --shell /bin/false prometheus
sudo chown -R prometheus:prometheus /etc/prometheus /var/lib/prometheus
sudo systemctl daemon-reload
sudo systemctl start prometheus
sudo systemctl enable prometheus
```

Access at http://localhost:9090.

## Method 2: Docker

```bash
docker run -d --name prometheus -p 9090:9090 \
  -v /path/to/prometheus.yml:/etc/prometheus/prometheus.yml \
  prom/prometheus
```

## Related Resources

- [Prometheus Tutorial](/blog/prometheus-tutorial-complete-guide-2026/)
- [Install Prometheus on Kubernetes with Helm](/blog/install-prometheus-kubernetes-helm/)
- [Prometheus PromQL Cheat Sheet](/blog/prometheus-query-cheat-sheet/)
