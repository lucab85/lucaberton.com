---
draft: false
title: "Install Terraform on macOS (Step-by-Step 2026)"
snippet: "Step-by-step guide to install Terraform on macOS. Copy-paste commands, verify installation, and configure for production."
description: "How to install Terraform on macOS in 2026. Complete step-by-step guide with commands, verification, and post-install configuration."
image:
  src: "/blog/thumbnails/install-terraform-macos.jpg"
  alt: "Install Terraform on macOS"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Automation"
tags: ["Terraform", "Installation", "macOS", "Tutorial", "DevOps"]
---
Here is how to install Terraform on macOS in 2026. Tested and verified.

## Method 1: Homebrew (Recommended)

```bash
brew tap hashicorp/tap
brew install hashicorp/tap/terraform
```

## Method 2: Direct Download

```bash
# Download (check https://releases.hashicorp.com/terraform/ for latest)
curl -LO https://releases.hashicorp.com/terraform/1.9.0/terraform_1.9.0_darwin_arm64.zip
unzip terraform_1.9.0_darwin_arm64.zip
sudo mv terraform /usr/local/bin/
rm terraform_1.9.0_darwin_arm64.zip
```

## Verify

```bash
terraform --version
```

## Enable Shell Completion

```bash
terraform -install-autocomplete
```

## Quick Start

```bash
mkdir ~/terraform-demo && cd ~/terraform-demo

cat > main.tf << 'EOF'
terraform {
  required_providers {
    local = {
      source = "hashicorp/local"
    }
  }
}

resource "local_file" "hello" {
  content  = "Hello, Terraform!"
  filename = "${path.module}/hello.txt"
}
EOF

terraform init
terraform plan
terraform apply -auto-approve
cat hello.txt
terraform destroy -auto-approve
```

## Related Resources

- [Terraform Cheat Sheet](/blog/terraform-cheat-sheet-2026/)
- [Install Terraform on Windows](/blog/install-terraform-windows/)
- [Terraform vs OpenTofu](/blog/terraform-vs-opentofu-2026/)
