---
title: "RHEL AI 3.4 on AWS Marketplace: What's New & Upgrade"
snippet: "Red Hat Enterprise Linux AI 3.4.0 is now on AWS Marketplace. What changed, how to upgrade running instances, and what it means for enterprise AI."
description: "Red Hat Enterprise Linux AI 3.4.0 is now available on AWS Marketplace. Here is what changed, how to upgrade running instances, and what the new version means for enterprise AI workloads."
publishDate: "2026-06-02"
lastModified: "2026-06-02"
image:
  src: "/blog/thumbnails/rhel-ai-deployment.jpg"
  alt: "Red Hat Enterprise Linux AI 3.4 AWS Marketplace deployment"
tags: ["rhel ai", "aws", "red hat", "ai platform", "enterprise ai"]
category: "AI"
author: "Luca Berton"
draft: false
---

Red Hat just shipped **RHEL AI 3.4.0** (build `20260602-1780387621`) on AWS Marketplace. If you are already running RHEL AI on EC2, your existing instances are unaffected — but upgrading is recommended.

## What is RHEL AI

Red Hat Enterprise Linux AI is a purpose-built operating system image that packages everything you need to run AI inference and fine-tuning workloads:

- **InstructLab** for synthetic data generation and model alignment
- **vLLM** for high-throughput inference serving
- **Granite models** pre-loaded and ready to serve
- **GPU drivers** (NVIDIA CUDA) pre-configured
- **SELinux** and container isolation enabled by default

It ships as an AMI on AWS Marketplace, a QCOW2 for on-prem, and ISOs for bare-metal deployment.

## What Changed in 3.4

While Red Hat has not published detailed release notes for this specific build, version 3.4 follows the rapid cadence established since RHEL AI 1.1 in late 2024. Based on the Red Hat AI platform roadmap shared at Summit 2026:

- **InstructLab updates** — improved synthetic data generation pipelines and model evaluation tooling
- **vLLM performance** — latest upstream vLLM with better throughput on NVIDIA GPUs
- **Security patches** — RHEL 9 base layer with all current CVE fixes
- **Granite model refresh** — newer model checkpoints with improved reasoning capabilities
- **bootc image mode** — immutable OS updates via `bootc upgrade` for atomic rollbacks

## How to Upgrade on AWS

### Option 1: Launch a New Instance

The simplest path — launch the new AMI and migrate your workload:

```bash
# Find the new RHEL AI 3.4 AMI from Marketplace
aws ec2 describe-images \
  --owners aws-marketplace \
  --filters "Name=name,Values=*RHEL-AI*3.4*" \
  --query 'Images[].{ID:ImageId,Name:Name,Date:CreationDate}' \
  --output table
```

Or visit the [AWS Marketplace listing](https://aws.amazon.com/marketplace/pp/prodview-vc3qboqpcobr6) and click **Launch**.

### Option 2: In-Place Upgrade with bootc

If your instance supports image mode (RHEL AI 1.3+):

```bash
# SSH into your running instance
ssh -i your-key.pem cloud-user@your-instance-ip

# Check current version
cat /etc/redhat-release
ilab --version

# Pull and stage the new image
sudo bootc upgrade

# Reboot to apply
sudo systemctl reboot
```

After reboot, verify:

```bash
cat /etc/redhat-release
rpm -q vllm instructlab
```

### Option 3: Keep Running (No Action Required)

Your existing subscription and running instances are unaffected. Previous versions remain accessible. Only upgrade when your maintenance window allows.

## SSH Access Reminder

RHEL AI on AWS uses:

- **Username:** `cloud-user` (not `ec2-user`, not `root`)
- **Auth:** SSH key pair you selected at launch
- **Root:** Disabled by default (use `sudo`)

```bash
# Set permissions on your key
chmod 400 your-key.pem

# Connect
ssh -i your-key.pem cloud-user@ec2-xx-xx-xx-xx.compute.amazonaws.com
```

## Instance Sizing for RHEL AI

For inference workloads with Granite models:

| Instance Type | GPUs | VRAM | Best For |
|---------------|------|------|----------|
| `g5.xlarge` | 1x A10G | 24 GB | Small models, development |
| `g5.12xlarge` | 4x A10G | 96 GB | Granite 7B inference |
| `p4d.24xlarge` | 8x A100 | 320 GB | Fine-tuning, large models |
| `p5.48xlarge` | 8x H100 | 640 GB | Production training |

## Why This Matters

RHEL AI is Red Hat's answer to the "AI platform fragility" problem. Instead of stitching together drivers, frameworks, and serving layers yourself, you get a tested, supported stack that upgrades atomically.

Combined with [InstructLab fine-tuning](/blog/fine-tuning-models-with-instructlab-on-rhel-ai/) and [Ansible-driven deployments](/blog/ansible-rhel-ai-platform-deployment/), version 3.4 continues the trajectory toward enterprise AI that is as manageable as any other RHEL workload.

## Related Content

- [Getting Started with RHEL AI: Installation and GPU Setup](/blog/getting-started-with-rhel-ai-installation-and-gpu-setup/)
- [Fine-Tuning Models with InstructLab on RHEL AI](/blog/fine-tuning-models-with-instructlab-on-rhel-ai/)
- [RHEL AI Deployment Guide for Enterprise Production](/blog/rhel-ai-deployment-guide-enterprise-production-2026/)
- [Ansible Automation for RHEL AI Deployments](/blog/ansible-automation-for-rhel-ai-deployments/)
- [AI Governance and Security on RHEL AI](/blog/ai-governance-and-security-on-rhel-ai/)
