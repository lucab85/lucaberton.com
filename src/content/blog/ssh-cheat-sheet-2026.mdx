---
draft: false
title: "SSH Cheat Sheet: Tunnels, Keys, and Config 2026"
snippet: "Comprehensive comparison to help you choose the right tool. Features, performance, use cases, and recommendations."
description: "SSH Cheat Sheet: Tunnels, Keys, and Config 2026 — detailed comparison of features, performance, use cases, pricing, and when to use each tool."
image:
  src: "/blog/thumbnails/ssh-cheat-sheet-2026.jpg"
  alt: "SSH Cheat Sheet: Tunnels, Keys, and Config 2026"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "DevOps"
tags: ["Comparison", "DevOps", "DevOps", "2026"]
---
A quick reference for SSH — secure remote access and tunneling. Bookmark this page.

## Connecting

```bash
# Basic connection
ssh user@hostname
ssh -p 2222 user@hostname     # Custom port
ssh -i ~/.ssh/mykey user@host # Specific key

# Verbose (debugging)
ssh -v user@hostname
ssh -vvv user@hostname        # Maximum verbosity
```

## Key Management

```bash
# Generate key pair
ssh-keygen -t ed25519 -C "email@example.com"
ssh-keygen -t rsa -b 4096 -C "email@example.com"

# Copy public key to server
ssh-copy-id user@hostname
ssh-copy-id -i ~/.ssh/mykey.pub user@hostname

# Add key to agent
eval $(ssh-agent)
ssh-add ~/.ssh/id_ed25519
ssh-add -l                    # List keys in agent
```

## SSH Config (~/.ssh/config)

```
Host dev
    HostName 10.0.0.5
    User deploy
    Port 2222
    IdentityFile ~/.ssh/dev_key

Host prod-*
    User admin
    IdentityFile ~/.ssh/prod_key
    ProxyJump bastion

Host bastion
    HostName bastion.example.com
    User jumpuser

Host *
    ServerAliveInterval 60
    ServerAliveCountMax 3
    AddKeysToAgent yes
```

```bash
# Now just type:
ssh dev
ssh prod-web1
```

## Port Forwarding (Tunnels)

```bash
# Local port forward (access remote service locally)
ssh -L 8080:localhost:80 user@remote
ssh -L 5432:db.internal:5432 user@bastion
# Now: localhost:8080 → remote:80
# Now: localhost:5432 → db.internal:5432

# Remote port forward (expose local service remotely)
ssh -R 8080:localhost:3000 user@remote
# Now: remote:8080 → localhost:3000

# Dynamic SOCKS proxy
ssh -D 1080 user@remote
# Configure browser to use SOCKS5 proxy localhost:1080

# Tunnel in background
ssh -fNL 5432:db.internal:5432 user@bastion
```

## File Transfer

```bash
# SCP
scp file.txt user@host:/remote/path/
scp user@host:/remote/file.txt ./local/
scp -r directory/ user@host:/remote/path/

# rsync (preferred for large transfers)
rsync -avz --progress local/ user@host:/remote/path/
rsync -avz --delete local/ user@host:/remote/path/  # Mirror
```

## Security Hardening

```bash
# /etc/ssh/sshd_config
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes
MaxAuthTries 3
AllowUsers deploy admin
Protocol 2
```

## Tips and Tricks

- Use `~/.ssh/config` to avoid typing long commands
- Use `ssh-agent` forwarding (`-A`) carefully — it exposes your keys on the remote host
- Use `ProxyJump` (not `ProxyCommand`) for bastion/jump hosts
- Use `ControlMaster` and `ControlPath` for connection multiplexing (faster subsequent connections)
- Use `mosh` for unreliable connections (survives network changes)

## Related Resources

- [Linux Cheat Sheet](/blog/linux-cheat-sheet-2026/)
- [OpenSSL Cheat Sheet](/blog/openssl-cheat-sheet-2026/)
- [Bash Cheat Sheet](/blog/bash-cheat-sheet-2026/)
