---
draft: false
title: "Terraform Modules Best Practices: Reusable Infrastructure Code"
snippet: "Terraform modules best practices. Structure, versioning, registry publishing, composition patterns, and testing with Terratest."
description: "Terraform modules best practices. Structure, versioning, registry publishing, composition patterns, and testing with Terratest."
image:
  src: "/blog/thumbnails/terraform-modules-best-practices-2026.jpg"
  alt: "Terraform Modules Best Practices: Reusable Infrastructure Code"
publishDate: "2026-04-04"
lastModified: "2026-04-04"
author: "Luca Berton"
category: "Automation"
tags: ["terraform modules", "Tutorial", "2026", "Automation", "DevOps"]
---

Terraform is my go-to for infrastructure provisioning. After managing hundreds of environments across AWS, Azure, and GCP, here is the practical guide.

## Why This Matters

Infrastructure as Code is not optional anymore. Manual provisioning does not scale, is not auditable, and leads to configuration drift.

## Prerequisites

- Terraform installed ([Install Terraform on Ubuntu](/blog/install-terraform-ubuntu-2404/) or [macOS](/blog/install-terraform-macos/))
- Cloud provider account (AWS, Azure, or GCP)
- Text editor (VS Code recommended)

## Step-by-Step Guide

### Step 1: Initialize Your Project

```hcl
# main.tf
terraform {
  required_version = ">= 1.7"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
  backend "s3" {
    bucket = "my-terraform-state"
    key    = "prod/terraform.tfstate"
    region = "eu-west-1"
  }
}

provider "aws" {
  region = var.aws_region
}
```

### Step 2: Write Resources

```hcl
# vpc.tf
resource "aws_vpc" "main" {
  cidr_block           = var.vpc_cidr
  enable_dns_hostnames = true
  enable_dns_support   = true

  tags = {
    Name        = "${var.project}-vpc"
    Environment = var.environment
    ManagedBy   = "terraform"
  }
}
```

### Step 3: Plan and Apply

```bash
# Initialize
terraform init

# Preview changes
terraform plan -out=tfplan

# Apply
terraform apply tfplan
```

## Common Mistakes

1. Not using remote state — local state files get lost or conflict
2. Hardcoding values — use variables and tfvars files
3. No state locking — concurrent applies corrupt state
4. Giant monolithic configs — use modules for reusability

## Related Resources

- [Terraform Cheat Sheet](/blog/terraform-cheat-sheet-2026/)
- [Terraform vs Ansible](/blog/terraform-vs-ansible-when-to-use-which-2026/)
- [Terraform vs Pulumi](/blog/terraform-vs-pulumi-2026/)
- [Terraform Modules Best Practices](/blog/terraform-modules-best-practices-2026/)
- [Terraform State Management](/blog/terraform-state-management-guide-2026/)

## About the Author

I am Luca Berton, AI and Cloud Advisor with 8 published books on automation, Kubernetes, and AI. [Book a consultation](https://calendly.com/lucaberton/) to discuss your terraform modules strategy.
